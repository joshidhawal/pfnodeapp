{
  "name": "pfnodeapp",
  "version": "1.0.0",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "dev": "set NODE_ENV=development&& nodemon -e ts --exec \"rm -rf dist/* && tsc -p . && npm run start\"",
    "start": "node dist/app.js",
    "prod": "set NODE_ENV=production&& npm run start",
    "tsWatch": "tsc --watch",
    "tsBuild": "rm -rf dist/* && tsc -p .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "schema-check": "typeorm schema:log -d dist/data-source.js",
    "schema-build-check": "npm run build && npm run typeorm-schema-check"
  },
  "keywords": [],
  "author": "Dhawal Joshi",
  "license": "SEE LICENSE in license.md",
  "description": "Node Companion App",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^22.18.6",
    "@types/sqlite3": "^3.1.11",
    "nodemon": "^3.1.7",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.0.1",
    "pg": "^8.16.3",
    "reflect-metadata": "^0.2.2",
    "sqlite3": "^5.1.7",
    "typeorm": "^0.3.26"
  }
}
