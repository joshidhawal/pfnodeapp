export type configTypes = {
  SERVER_PORT: number;
  SERVER_HOST: string;
  JWT_SECRET: string;
  DB_TYPE: string;
  DB_USER: string;
  DB_SCHEMA: string;
  DB_PASSWORD: string;
  DB_PORT: number;
  DB_HOST: string;
};
